[gd_scene load_steps=6 format=2]

[ext_resource path="res://assets/debug/pixel.png" type="Texture" id=1]
[ext_resource path="res://assets/light mask/radial_light_mask.png" type="Texture" id=2]
[ext_resource path="res://assets/bg/starfield.png" type="Texture" id=3]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform sampler2D noise_text;

void fragment() {
	vec3 col = vec3(1.0, 0.9, 0.3);
	
	vec3 n = texture(noise_text, UV).xyz;
	
	float a = clamp(1.2 / distance(UV, vec2(0.5, 0.5)) - 2.6, 0.0, 1.0);
	
	col.gb -= n.r * a + 0.3;
	
	COLOR = vec4(col, a);
}"

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 1 )
shader_param/noise_text = ExtResource( 3 )

[node name="Star" type="Node2D"]

[node name="Sprite" type="Sprite" parent="."]
light_mask = 0
material = SubResource( 2 )
scale = Vector2( 512, 512 )
texture = ExtResource( 1 )

[node name="Light2D" type="Light2D" parent="."]
texture = ExtResource( 2 )
range_height = 32.0
shadow_enabled = true
shadow_gradient_length = 10.0
shadow_filter = 3
shadow_filter_smooth = 5.0
